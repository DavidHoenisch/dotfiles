#!/usr/bin/env bash

file=$(/bin/eza -liR ~/.local/bin/custom/ | awk '{print $NF}' | xargs -I{} realpath "$HOME/.local/bin/custom/{}" 2>/dev/null | fzf --preview='/bin/cat {}' 2>/dev/null)

if [[ -z "$file" ]]; then
  exit 1
else
  "$file"
fi
