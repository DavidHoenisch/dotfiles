export EDITOR=nvim
#
#export $(cat ~/.env.creds|xargs)
export PAGER="less"

plugins=(git)

alias conf="nvim ~/.config/nvim/"
alias gf="cd ~/Documents/Github/"
alias ls="$(which eza)"
alias ll="$(which eza) -liR"
alias work="~/.local/bin/work"
alias lz="lazygit"
alias zz="nvim $HOME/.zshrc"
alias ref="source $HOME/.zshrc"
alias cat="bat"
alias cc="/bin/cat"
alias c="/bin/cd"
alias copy="fc -ln -1|wl-copy"
alias gr="goreleaser"
alias rm="echo Use 'rip' instead of rm."

# tmux binds
alias ta="tmux attach-session -t"
alias tls="tmux ls"
alias tks="tmux kill-server"
alias td="tmux detach"
alias tfi="tmuxifier"
alias rn="ranger"

alias tt="timedatectl"
alias nf="/bin/bash $HOME/.local/bin/nf"
alias oll="/bin/bash $HOME/.local/bin/oserve"
alias sp="ps aux | fzf"
autoload -Uz compinit
zstyle ':completion:*' menu select
fpath+=~/.zfunc

# added by Webi for pyenv
eval "$(starship init zsh)"

eval "$(zoxide init --cmd cd  zsh)"


# added by Webi for pyenv
export PATH="$PATH:$HOME/.local/share/bob/nvim-bin"
export PATH="$PATH:$HOME/.tmux/plugins/tmuxifier/bin/"

export FZF_COMPLETION_TRIGGER='~~'




######################
# Kubernetes Configs #
######################

export KIND_EXPERIMENTAL_PROVIDER=podman

# export creds
source ~/.creds.env

source ~/.hisconfig

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# Generated for envman. Do not edit.
[ -s "$HOME/.config/envman/load.sh" ] && source "$HOME/.config/envman/load.sh"
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"
eval "$(tmuxifier init -)"


# shortcut keys
list_zsh_alias() {
	local command="$HOME/.local/bin/what/zshshortcut"
	eval $command
}

zle -N list_zsh_alias

bindkey "^A" list_zsh_alias


start_ollama() {
	local command="$HOME/.local/bin/what/oserve"
	eval $command
}

zle -N start_ollama

bindkey "^o" start_ollama

search_open_nvim() {
	local command="$HOME/.local/bin/custom/nf"
	eval $command
}

zle -N search_open_nvim

bindkey "^f" search_open_nvim


what_script() {
	local command="$HOME/.local/bin/custom/what"
	eval $command
}

zle -N what_script

bindkey "^w" what_script

# Snippets manager

pet_search_and_run() {
	local command="$HOME/.local/bin/custom/pets"
	eval $command
}

zle -N pet_search_and_run
bindkey "^p" pet_search_and_run


# espanso reload
restart_espanso() {
	local command="espanso restart"
	eval $command
}

zle -N restart_espanso
bindkey "^e" restart_espanso


$HOME/.local/bin/tl

eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"


# Generated for pdtm. Do not edit.
export PATH=$PATH:/home/dhoenisch/.pdtm/go/bin


# Generated by Webi. Do not edit.
eval "$(webi --init zsh)"


export PYENV_ROOT="$HOME/.pyenv"
[[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
eval "$(pyenv init -)"


# Yazi Configuration

function yy() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
	yazi "$@" --cwd-file="$tmp"
	if cwd="$(cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
		cd -- "$cwd"
	fi
	rm -f -- "$tmp"
}

# setting for fuz
alias fuz='/home/dhoenisch/.local/bin/fuz --path "/home/dhoenisch/Documents/Personal"'
alias fz='fuz --sorttime'
alias fze='fuz --edit'
alias fzd='fuz --dir-search'
export FUZ_EDITOR=nvim

. "$HOME/.atuin/bin/env"

eval "$(atuin init zsh)"

export PATH="$PATH:$HOME/.local/bin/custom/"


export LANG=en_US.UTF-8
export LANGUAGE=en_US.UTF-8
export LC_ALL=en_US.UTF-8


# age encryption #
export AGE_PUB_KEY=$(grep 'public key' $HOME/.config/sops/age/keys.txt | cut -d' ' -f 4)
